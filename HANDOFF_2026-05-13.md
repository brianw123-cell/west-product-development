# West Product Development website — session handoff

**Date:** 2026-05-13
**Repo:** https://github.com/brianw123-cell/west-product-development
**Live site:** https://westproductdev.com

## What this session shipped to production

All changes merged into `main` and deployed via Vercel. Final state at commit `58d1335`.

### Bug fix
- Removed stray `blockquote></cite></p>` text rendering on the AI Workshop card

### Layout & spacing
- Hero trimmed in two passes (~30% then another 25%) — padding, eyebrow margin, h1 margin, logo height all reduced
- Contact section converted to one-row flex: "Get in touch" left, three contact cards right
- Footer dark navy background restored (root cause: missing semicolon in `:root` CSS block was breaking the `--navy` variable parse)
- Team-card pills aligned to the bottom on both cards (flex-column + `margin-top: auto`)

### Copy & positioning
- Mission rewritten to lead with renewable energy / solar / climate-sector focus
- "Sonoma County Transportation Climate Authority" → "Sonoma County Transportation **and** Climate Authorities" (plural)
- Status badge "In planning" → "Pilot phase"
- "View case study →" replaced with "Petaluma, CA · 2026" on both featured cards

### Visuals — Option A (device frames)
- **Digital Proposal card** — Personalized Proposal mobile screenshot inside a phone bezel (with notch indicator)
- **Field Operations Portal card** — admin dashboard inside a Mac-style browser chrome (traffic-light dots + URL bar)
- **SCTCA card** — Climate Action Tracker inside browser chrome
- **AI Workshop card** — 8 Levels of AI Mastery infographic (with WEST branding) in contain mode

### Card order (final)
1. Amy's R+S — Digital Proposal Ecosystem
2. Amy's R+S — Field Operations Portal
3. Sonoma County Transportation and Climate Authorities
4. AI Automation Workshop — Solar Industry

### Files added to repo
- `ai-mastery.png` — the 8 Levels of AI Mastery infographic
- `proposal-mobile.png` — Personalized Proposal iOS screenshot (was `IMG_4256.PNG` in Downloads)
- `field-ops-dashboard.png` — narrower version of the Amy's portal dashboard
- `sctca-dashboard.png` — Climate Action Tracker dashboard

## Git state at end of session
- `main` is clean, fully pushed at commit `58d1335`
- `preview/visuals` branch still exists — can be deleted whenever (it was the working branch for all of this)

## Vercel
- Two production deployments fired on push to main today
- Westproductdev.com (custom domain) auto-aliased to the latest production deploy

## Things to test
- **Mobile viewport** — Brian asked whether the site works on phones. Responsive rules at `@media (max-width: 900px)` and `(max-width: 600px)` were written but not visually tested on a real mobile device. Worth pulling up westproductdev.com on his iPhone to confirm — especially the phone-bezel card (a phone-shape inside a phone screen could read as charming or redundant).

## Items deferred / for future sessions

**From the project survey done mid-session** (agent looked across `~/Documents/Claude Code/*`):

- **Database Digital Proposal WebApp** (Next.js/Supabase rebuild of the proposal app — still in design phase). Could become a 5th project card once Phase 2 ships. The current Digital Proposal card covers the Apps Script/AppSheet v1.
- **SCTCA card refinement** — agent noted it's actually deployed as a public BETA MVP, not just "in planning." Already updated to "Pilot phase" this session. Could be refined further once the client decides next-year-budget.

**Things to skip:**
- Coastside Clean Energy (empty repo)
- Solar Installation Machines (standalone HTML utilities)
- Starling (archived prototypes)
- OpenBrainMyBrain (personal productivity, off-brand)
- Bidding Tool App (same as Apps Script ecosystem card)

## Other workspace state

- **No dev servers running** (`lsof -iTCP -sTCP:LISTEN` returned no node/next processes)
- **Amys-R-S repo** has ~31 macOS Finder duplicate files (`* 2.sql`, `* 2.md`) untracked. These are noise — Finder auto-creates them when files are copied with name collisions. Worth cleaning up next time you're in that repo: `cd ~/Documents/Claude\ Code/Amys-R-S && git status | grep " 2\." | wc -l` to count, then `rm` them.
- One genuine new file in Amys-R-S: `docs/PORTAL_INSTALL.pdf` (untracked). Not touched this session.

## Pick-up prompt for next chat

> Open westproductdev.com — production state at commit 58d1335 (May 13, 2026). All four project cards now use Option A device frames (phone bezel for mobile, browser chrome for desktop, plain contain for infographic). Last open question: mobile-viewport visual test. Otherwise the site is shipped.
